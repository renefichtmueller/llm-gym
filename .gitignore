# Secrets / local config — never commit
.env
.secret.key
*.key
*.pem
secrets.json

# Local gym state
settings.json
gym.db
gym.db-*
.gym_instance.lock
adapters/*.json
adapters/*/
!adapters/.gitkeep
runs/
logs/

# Local editor/tooling config (not part of the project itself)
.claude/

# Training pool data (keep the folder, not the data)
data/pool/**
!data/pool/.gitkeep

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
build/
dist/

# OS / editor
.DS_Store
.idea/
.vscode/
